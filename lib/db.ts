import 'server-only';

// This is a placeholder file for your database queries.
// You can create functions to interact with your database here.
//
// Example:
//
// import { db } from './drizzle'; // Assuming you set up your Drizzle client in 'lib/drizzle.ts'
// import { notes } from './schema';
// import { eq } from 'drizzle-orm';
//
// export async function getNotes() {
//   return await db.select().from(notes);
// }
//
// export async function getNoteById(id: number) {
//   return await db.select().from(notes).where(eq(notes.id, id));
// }
